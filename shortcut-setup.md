# ショートカット「レシピかごに送る」の作り方

写真アプリでレシピの写真を選んで共有するだけで、材料が取り出された状態で
「レシピかご」が開くようにします。**作るのは最初の1回だけ**です。

読み取りはiPhoneの中で行われます。通信もAPIキーもいりません。

作るアクションは**5つ**。慣れていなくても15分ほどでできます。

---

## はじめに読んでください

### アクションを「コピー＆ペースト」しないでください

ショートカットは、アクションをコピーすると**元の参照先を覚えたまま**貼り付きます。
見た目は同じなのに中身が違う、という状態になり、原因の分からない不具合になります。
同じようなアクションでも、必ず1つずつ追加してください。

### 用語

| 言葉 | 意味 |
|---|---|
| **アクション** | 1つの命令。これを上から順に並べます |
| **青いピル** | アクションの中にある、丸みのある青い部品。前のアクションの結果を指します |
| **変数** | 前のアクションの結果につけられた名前 |

### 準備

このショートカットは「レシピかごを開く」という命令で終わります。
そのため、レシピかごの住所（URL）が必要です。

```
https://libertedebut.github.io/recipe-kago/
```

**これがレシピかごの住所です。** 手順7でこのとおりに入力します。
自分で別の場所に置いている場合は、その住所に読み替えてください。

---

## 手順1　新しいショートカットを作る

1. ホーム画面で **「ショートカット」** アプリを開きます
   （見つからないときは、ホーム画面を下にスワイプして「ショートカット」と検索）
2. 下のタブが **「ショートカット」** になっていることを確認します
3. 右上の **＋** を押します
4. 「新規ショートカット」という空の画面になります

---

## 手順2　共有シートに出るようにする

これをしないと、写真アプリの共有メニューに出てきません。

1. 画面の下のほう、または上部にある **「i」（丸にi）** のボタンを押します
2. 設定の一覧が出ます。**「共有シートに表示」** のスイッチを **オン**（緑）にします
3. オンにすると、すぐ下に **「共有シートのタイプ」** という項目が現れます。押します
4. たくさんの種類が並んでいます。**「イメージ」だけをオン**にして、
   **それ以外は全部オフ**にします

   > 全部オフにするのが面倒なら、右上に「なし」や「すべて選択解除」があれば
   > それを押してから「イメージ」だけをオンにしてください。
   > こうすると、写真を選んだときだけメニューに出てきます。

5. 「完了」を押して元の画面に戻ります

この時点で、一番上に **「ショートカットの入力を受け取る」** のような行が
自動で追加されます。これはそのままにしてください。

---

## 手順3　画像から文字を取り出す（アクション1つめ）

1. 画面下の **検索欄**（「アクションやAppを検索」）をタップします
2. **「テキストを抽出」** と入力します
3. 候補に出てくる **「イメージからテキストを抽出」**（または「画像からテキストを抽出」）
   をタップします

   > 名前は iOS のバージョンで少し違います。**「〜からテキストを抽出」**
   > という形のものを選んでください。カメラやPDFではなく、**イメージ／画像**のものです。

4. 追加されたアクションを見ます。こう書かれているはずです

   ```
   ［ショートカットの入力］からテキストを抽出
   ```

5. 青いピルが **「ショートカットの入力」** になっていることを確認します。
   違うものになっていたら、青いピルをタップして
   **「ショートカットの入力」** を選び直してください

> **これがiPhone内蔵の文字認識です。** 日本語も読めます。通信はしません。
> 写真を複数選んでも、1枚ずつ自動で処理されます。

---

## 手順4　レシピの境目を作ってつなぐ（アクション2つめ）

写真1枚＝1レシピとして扱うために、境目の目印を入れます。

1. 検索欄に **「結合」** と入力します
2. **「テキストを結合」** をタップします
3. 追加されたアクションはこうなっています

   ```
   ［抽出されたテキスト］を 改行 で結合
   ```

4. 青いピルが **「抽出されたテキスト」** になっているか確認します。
   なっていなければ、タップして選び直します
5. **「改行」の部分をタップ**します。選択肢が出ます
6. **「カスタム」** を選びます
7. 入力欄が出るので、次を**そのまま**入力します

```
@@@RECIPE@@@
```

   > 半角のアットマーク3つ、`RECIPE`（大文字）、アットマーク3つです。
   > ここがレシピの境目になります。

---

## 手順5　ここで一度、動作を確認します

先へ進む前に、**本当に文字が読めているか**を確かめます。
ここを飛ばすと、後で原因が分からなくなります。

1. 検索欄に **「クイックルック」** と入力し、**「クイックルック」** を追加します
   （一番下に追加されます）
2. 右上の **▶（再生）** ボタンを押します
3. 写真を選ぶ画面が出たら、**レシピの写真を2枚**選びます
4. 画面に、読み取られた文字が表示されます

**確認すること**

- 材料の名前と分量が読めているか
- 2枚目のレシピとの間に `@@@RECIPE@@@` が入っているか

読めていれば成功です。**「クイックルック」のアクションを削除してください**
（アクションを左にスワイプするか、長押しして「削除」）。

読めていない場合は、この記事の最後「うまくいかないとき」を見てください。

---

## 手順6　Base64にする（アクション3つめ）

読み取った文字を、URLに乗せられる形に変換します。

1. 検索欄に **「Base64」** と入力します
2. **「Base64エンコード」** をタップします

   > 追加されると「テキストをBase64エンコード」のような表示になります。

3. 青いピルが **「結合されたテキスト」** になっていることを確認します
4. アクションの左端にある **「>」** を押して詳細を開きます
5. **「改行」** という項目を **「なし」** にします

   > 改行が入っていても動くように作ってありますが、なしのほうが確実です。

---

## 手順7　URLを組み立てる（アクション4つめ）

ここが一番間違えやすいところです。ゆっくりやってください。

1. 検索欄に **「テキスト」** と入力します
2. 一番上に出てくる **「テキスト」** をタップします
   （「テキストを結合」ではなく、ただの「テキスト」です）
3. 追加された**空の入力欄をタップ**して、カーソルを出します
4. まず、URLを**手で入力**します（変数はまだ入れません）

```
https://libertedebut.github.io/recipe-kago/#r=
```

   > 最後は `#r=` で終わります。この後ろに変数をつなげます。
   > **末尾に余計な空白や改行を入れないでください。**

5. カーソルが `=` の**すぐ右**にあることを確認します
6. キーボードの**すぐ上**に、青い変数のボタンが並んだ帯があります。
   その中から **「Base64エンコードされたテキスト」** を探してタップします

   > 帯が見当たらない場合は、入力欄をもう一度タップするとキーボードと一緒に出ます。
   > 変数が多くて見つからないときは、帯の中を横にスクロールしてください。

7. 完成すると、こう見えます

```
https://libertedebut.github.io/recipe-kago/#r=［Base64エンコードされたテキスト］
```

   `［ ］` の部分が青いピルになっていれば正解です。
   **文字として「Base64エンコードされたテキスト」と書かれていたら間違い**です。
   その場合は消して、6からやり直してください。

---

## 手順8　URLを開く（アクション5つめ）

1. 検索欄に **「URLを開く」** と入力します
2. **「URLを開く」** をタップします
3. 青いピルが **「テキスト」** になっていることを確認します

   > ここが「URL」や別のものになっていたら、タップして
   > **「テキスト」**（手順7で作ったもの）を選び直してください。

---

## 手順9　名前を付ける

1. **「i」** ボタンを押します
2. 一番上の名前の欄に **「レシピかごに送る」** と入力します
3. 好みでアイコンや色も変えられます（変えなくても動きます）
4. 「完了」を押します

これで完成です。

---

## できあがりの形

上から順に、こうなっていれば正解です。

```
1  ショートカットの入力を受け取る（イメージ）
2  ［ショートカットの入力］からテキストを抽出
3  ［抽出されたテキスト］を @@@RECIPE@@@ で結合
4  ［結合されたテキスト］をBase64エンコード（改行：なし）
5  テキスト
   https://libertedebut.github.io/recipe-kago/#r=［Base64エンコードされたテキスト］
6  ［テキスト］を開く
```

---

## 使ってみる

1. **写真アプリ**を開きます
2. 右上の「選択」を押して、**レシピの写真を複数選びます**
3. 左下の **共有ボタン**（四角に上矢印）を押します
4. メニューを下にスクロールして **「レシピかごに送る」** を押します
5. Safariが開き、材料が取り出された状態の「レシピかご」が出ます
6. 料理名や材料を確認・修正して **「リストに追加」** を押します

初回だけ「このショートカットは…を許可しますか？」と聞かれたら「許可」を選んでください。

---

## うまくいかないとき

### 共有メニューに「レシピかごに送る」が出ない

手順2の**「共有シートに表示」がオン**になっているか確認してください。
また「共有シートのタイプ」で**「イメージ」がオン**になっている必要があります。

### 手順5のクイックルックで、文字が何も出ない

- 写真に**材料の欄が写っている**か確認してください
- ピントが合っているか、影が濃すぎないか確認してください
- アクション2の青いピルが「ショートカットの入力」になっているか確認してください

### クイックルックで、1枚目のレシピしか出ない

複数の写真が1枚ずつ処理されていません。次のようにしてください。

1. アクション2の**上**に「各項目を繰り返す」を追加します
   （検索欄に「繰り返す」と入力）
2. 繰り返す対象を **「ショートカットの入力」** にします
3. アクション2（テキストを抽出）を、繰り返しの**内側**に移動します
   （アクションを長押ししてドラッグ）
4. アクション2の青いピルを **「繰り返し項目」** に変えます
5. アクション3（結合）の青いピルを **「繰り返しの結果」** に変えます

### Safariは開くが、材料が取り出されていない

手順7のURLを見直してください。よくある間違いは3つです。

- `#r=` の `#` が抜けている
- `#r=` の後ろが**青いピルではなく、ただの文字**になっている
- URLの末尾に余計な空白や改行が入っている

### 「受け取った内容を読めませんでした」と出る

手順6のBase64エンコードの入力が、**「結合されたテキスト」**に
なっているか確認してください。別のものを指していることが多いです。

### 料理名が「レシピ 1」になる

写真に料理名が写っていないときはこうなります。画面上で直せます。

### 1つのレシピが2枚の写真にまたがっている

写真1枚が1レシピとして扱われるので、2つに分かれます。
取り込んだあとに**料理名を同じ名前にそろえる**と、1つにまとまります。
