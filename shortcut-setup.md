# ショートカット「レシピかごに送る」の作り方

写真アプリでレシピの写真を選んで共有するだけで、材料が取り出された状態で
「レシピかご」が開くようにします。**作るのは最初の1回だけ**です。

読み取りはiPhoneの中で行われます。通信もAPIキーもいりません。

自分で足すアクションは**5つ**。慣れていなくても20分ほどでできます。

この手順書は **iOS 26 の実機で1つずつ確認しながら書いています。**
古いiOSでは名前が違うことがあるので、そのときは「〜のようなもの」を探してください。

---

## はじめに読んでください

### アクションを「コピー＆ペースト」しないでください

ショートカットは、アクションをコピーすると**元の参照先を覚えたまま**貼り付きます。
見た目は同じなのに中身が違う、という状態になり、原因の分からない不具合になります。
同じようなアクションでも、必ず1つずつ追加してください。

### 検索結果は「1回だけ」タップしてください

検索して出てきたアクションを2回タップすると、**同じアクションが2つ追加されます。**
気づかずに進むと動きません。追加されたら一覧を目で数えてください。

### 用語

| 言葉 | 意味 |
|---|---|
| **アクション** | 1つの命令。これを上から順に並べます |
| **青いピル** | アクションの中にある、丸みのある青い部品。前のアクションの結果を指します |
| **編集画面** | アクションが縦に並んでいる画面。一番下に検索欄とツールバーがあります |

### レシピかごの住所（URL）

このショートカットは「レシピかごを開く」で終わります。そのため住所が必要です。

```
https://libertedebut.github.io/recipe-kago/
```

**これがレシピかごの住所です。** 手順7でこのとおりに入力します。
自分で別の場所に置いている場合は、その住所に読み替えてください。

---

## 手順1　新しいショートカットを作る

1. **「ショートカット」** アプリを開きます
   （見つからないときは、ホーム画面を下にスワイプして「ショートカット」と検索）
2. 下のタブが **「ショートカット」** になっていることを確認します
3. 右上の **＋** を押します
4. 「新規ショートカット」という空の編集画面になります

---

## 手順2　文字を読み取るアクションを入れる

設定より先に、中身を1つ入れます。

1. 画面下の **検索欄**（「アクションを検索」）をタップします
2. **「テキストを抽出」** と入力します
3. **「画像からテキストを抽出」** を**1回だけ**タップします

   > iOSによっては「イメージからテキストを抽出」という名前です。
   > **「〜からテキストを抽出」**で、カメラやPDFではないものを選びます。

これが**iPhoneに内蔵された文字認識**です。日本語も読めます。通信はしません。

---

## 手順3　共有シートに出るようにする

これをしないと、写真アプリの共有メニューに出てきません。

### 3-1　詳細を開く

編集画面の**一番下のツールバー**を見てください。5つのボタンが並んでいます。

```
   ↩       ↪       ⓘ       ⬆       ▶
  取消し   やり直し  詳細    共有    実行
```

**真ん中の ⓘ（丸にi）** を押します。「詳細」という画面が開きます。

> **上の名前の横にある「⌄」ではありません。** そちらは名称変更・アイコン・複製の
> メニューで、共有シートの設定は入っていません。

### 3-2　スイッチを入れる

「詳細」の中の **「共有シートに表示」** を **オン**（緑）にします。

**ここで「共有シートのタイプ」を探さないでください。この画面にはありません。**
種類の設定は、次の 3-3 でアクションとして現れます。

右上の**青い ✓** を押して詳細を閉じます。

### 3-3　受け取る種類を「画像」だけにする

編集画面に戻ると、**一番上に新しい行が増えています。**

```
「共有シート」から ［アプリ、その他18個］ を受け取る
もし入力がない場合: 続ける
```

1. **「アプリ、その他18個」の青い部分をタップ**します
2. 19種類がスイッチ付きで並びます
3. **「画像」だけをオン**にして、**それ以外は全部オフ**にします

   > 「イメージ」ではなく **「画像」** です。
   > 「メディア」は動画も含むので違います。「ファイル」も違います。

こうすると、**写真を選んだときだけ**共有メニューに出てきます。

### 3-4　入力のつながりを確認する

2つめのアクションを見てください。

```
［ショートカットの入力］からテキストを抽出
```

青いピルが **「ショートカットの入力」** になっていることを確認します。
空欄や別のものになっていたら、タップして選び直してください。

**ここが今回いちばん間違えやすい場所です。** 空のままだと、写真を渡しても何も読みません。

---

## 手順4　レシピの境目を作る

写真1枚＝1レシピとして扱うために、境目の目印を入れます。

1. 検索欄に **「結合」** と入力します
2. **「テキストを結合」** を**1回だけ**タップします
3. 追加された行はこうなっています

   ```
   ［画像からテキスト］を 改行 で結合
   ```

   > 青いピルが「画像からテキスト」と途中で切れて表示されますが、**これで正解**です。
   > 2つめのアクションの結果を指しています。

4. **「改行」の部分をタップ**します
5. **「カスタム」** を選びます
6. 入力欄が出るので、次を**そのまま**入力します

```
@@@RECIPE@@@
```

   > 半角のアットマーク3つ、`RECIPE`（すべて大文字）、アットマーク3つ。
   > **前後に空白を入れないでください。**

### ここまでの形

**アクションは3つ**です。4つあったら「テキストを結合」を2回押しています。
余分なほうの**右端の ⊗** で消してください。

```
1  「共有シート」から［画像］を受け取る
2  ［ショートカットの入力］からテキストを抽出
3  ［画像からテキスト］を カスタム @@@RECIPE@@@ で結合
```

---

## 手順5　ここで一度、動作を確認します

先へ進む前に、**本当に文字が読めているか**を確かめます。
ここを飛ばすと、後で原因が分からなくなります。

### 5-1　確認用のアクションを足す

検索欄に **「クイックルック」** と入力し、**「クイックルック」** を追加します。

### 5-2　名前を付ける

共有メニューで探せるように、先に名前を付けます。

1. 上の名前の横の **⌄** を押します
2. **「名称変更」** を選びます
3. **「レシピかごに送る」** と入力します

### 5-3　写真アプリから試す

**▶（実行）ボタンは使いません。**

▶ は編集画面から直接実行するボタンで、**共有シートを通らないため写真が渡りません。**
押すと「項目なし ─ プレビュー対象の項目が渡されませんでした」と出ます。故障ではありません。

正しい試し方はこちらです。

1. 左上の **＜** を押して編集画面を閉じます
2. **写真アプリ**を開きます
3. 右上の **「選択」** を押して、**レシピの写真を2枚**選びます
4. 左下の**共有ボタン**（四角に上矢印）を押します
5. メニューを**下にスクロール**して **「レシピかごに送る」** を押します

初回は「このショートカットは…を許可しますか？」と聞かれます。**「許可」** を選びます。

### 5-4　見てほしいこと

読み取られた文字が画面に出ます。確認するのは2点です。

- **材料の名前と分量**が読めているか（「小松菜・・・1束（200g）」のように）
- 1枚目と2枚目の間に **`@@@RECIPE@@@`** が入っているか

読めていれば成功です。**「クイックルック」のアクションを削除してください**
（右端の ⊗、またはアクションを長押しして「削除」）。

読めていない場合は、最後の「うまくいかないとき」を見てください。

---

## 手順6　Base64にする

読み取った文字を、URLに乗せられる形に変換します。

1. 検索欄に **「Base64」** と入力します
2. **「Base64エンコード」** をタップします
3. 追加された行はこうなります

   ```
   ［結合済みのテキスト］を base64で エンコード  ⟩
   ```

4. 青いピルが **「結合済みのテキスト」** になっていることを確認します
5. 右端の **⟩**（丸に矢印）を押して詳細を開きます
6. **「改行」** を **「なし」** にします

---

## 手順7　URLを組み立てる

ここが一番間違えやすいところです。ゆっくりやってください。

1. 検索欄に **「テキスト」** と入力します
2. 一番上に出てくる **「テキスト」** をタップします
   （「テキストを結合」ではなく、ただの「テキスト」です）
3. 追加された**空の入力欄をタップ**して、カーソルを出します
4. まず、URLを**手で入力**します（変数はまだ入れません）

```
https://libertedebut.github.io/recipe-kago/#r=
```

   > **`https` の `s` を忘れやすいので確認してください。**
   > `http` のままだと、転送の途中で `#` 以降が落ちることがあります。
   >
   > 最後は `#r=` で終わります。**末尾に空白や改行を入れないでください。**

5. カーソルが `=` の**すぐ右**にあることを確認します
6. キーボードの**すぐ上**に、青い変数のボタンが並んだ帯があります。
   その中から **「Base64」で始まるもの**をタップします

   > 帯が見当たらない場合は、入力欄をもう一度タップするとキーボードと一緒に出ます。
   > 変数が多いときは帯の中を横にスクロールしてください。

7. 完成すると、こう見えます

```
https://libertedebut.github.io/recipe-kago/#r=［Base64エンコード］
```

   `［ ］` の部分が**青いピル**になっていれば正解です。
   **文字として「Base64エンコード」と書かれていたら間違い**です。
   その場合は消して、6からやり直してください。

---

## 手順8　URLを開く

1. 検索欄に **「URLを開く」** と入力します
2. **「URLを開く」** をタップします
3. 青いピルが **「テキスト」** になっていることを確認します

   > ここが「URL」や別のものになっていたら、タップして
   > **「テキスト」**（手順7で作ったもの）を選び直してください。

---

## できあがりの形

上から順に、こうなっていれば完成です。

```
1  「共有シート」から［画像］を受け取る
   もし入力がない場合: 続ける
2  ［ショートカットの入力］からテキストを抽出
3  ［画像からテキスト］を カスタム @@@RECIPE@@@ で結合
4  ［結合済みのテキスト］を base64で エンコード（改行：なし）
5  テキスト
   https://libertedebut.github.io/recipe-kago/#r=［Base64エンコード］
6  ［テキスト］を開く
```

---

## 使ってみる

1. **写真アプリ**を開きます
2. 右上の「選択」を押して、**レシピの写真を複数選びます**
3. 左下の**共有ボタン**を押します
4. メニューを下にスクロールして **「レシピかごに送る」** を押します
5. Safariが開き、材料が取り出された状態の「レシピかご」が出ます
6. 料理名や材料を確認して **「リストに追加」** を押します

### 「分数の読み違いかも」が出たとき

`½缶` のような分数は、写真から読むと `12缶` のように数字2つになることがあります。
その形に当てはまる行にだけ、こういう印が出ます。

```
12缶(200g)
⚠ 分数の読み違いかも  1/2缶(200g) に直す
```

**押せば直ります。** 本当に12個ということもあるので、勝手には変えません。
押さなければそのままです。**印が付いていない行は確認しなくて大丈夫です。**

### 料理名について

雑誌の見開きなど、見出しが「鮭と小松菜の」「中華蒸し」と2行に割れて読まれる場合は、
自動でつなぎます。それでも外れることはあるので、そのときは画面で直してください。

---

## うまくいかないとき

### 共有メニューに「レシピかごに送る」が出ない

手順3-2の**「共有シートに表示」がオン**か確認してください。
また手順3-3で**「画像」がオン**になっている必要があります。

### ▶ を押すと「項目なし」と出る

**正常です。** ▶ は共有シートを通らないので写真が渡りません。
手順5-3のとおり、**写真アプリの共有メニューから**起動してください。

### 「詳細」の中に「共有シートのタイプ」がない

**この iOS ではそこにありません。** 「共有シートに表示」をオンにすると、
編集画面の**一番上のアクション**として現れます（手順3-3）。

### 上の「⌄」を押しても「詳細」が出ない

**⌄ には入っていません。** 編集画面の一番下のツールバーにある
**ⓘ** を押してください（手順3-1）。

### クイックルックで、文字が何も出ない

- 写真に**材料の欄が写っている**か確認してください
- ピントが合っているか、影が濃すぎないか確認してください
- 手順3-4の青いピルが**「ショートカットの入力」**になっているか確認してください

### クイックルックで、1枚目のレシピしか出ない

複数の写真が1枚ずつ処理されていません。次のようにしてください。

1. アクション2の**上**に「各項目を繰り返す」を追加します（検索欄に「繰り返す」）
2. 繰り返す対象を **「ショートカットの入力」** にします
3. アクション2を、繰り返しの**内側**に移動します（長押ししてドラッグ）
4. アクション2の青いピルを **「繰り返し項目」** に変えます
5. アクション3の青いピルを **「繰り返しの結果」** に変えます

### Safariは開くが、材料が取り出されていない

手順7のURLを見直してください。よくある間違いは4つです。

- `https` の **`s` が抜けている**
- `#r=` の `#` が抜けている
- `#r=` の後ろが**青いピルではなく、ただの文字**になっている
- URLの末尾に余計な空白や改行が入っている

### 「受け取った内容を読めませんでした」と出る

手順6のBase64エンコードの入力が **「結合済みのテキスト」** に
なっているか確認してください。別のものを指していることが多いです。

### 材料名の頭に「A」が付いている、分量がおかしい

レシピかご側で直しています。Safariのページを**再読み込み**してから
もう一度送ってみてください。

### 1つのレシピが2枚の写真にまたがっている

写真1枚が1レシピとして扱われるので、2つに分かれます。
取り込んだあとに**料理名を同じ名前にそろえる**と、1つにまとまります。

---

# もう1つ：レシピサイト用（PDF版）

写真から読み取ると、どうしても読み違いが出ます。**レシピサイトなら、文字認識を通さずに取り込めます。**

Safariでレシピのページを開き、**スクリーンショット → フルページ → ファイルに保存**でPDFにします。
このPDFには**文字がそのまま入っている**ので、読み違いがゼロになります。

見つけたときに保存しておいて、買い物の前にまとめて取り込む、という使い方ができます。

## 作り方（5つ）

**1** ショートカットアプリ → 右上の **＋**

**2** 検索欄に **`PDF`** → **「PDFからテキストを取得」**

**3** 下のツールバーの **ⓘ** → **「共有シートに表示」をオン** → 青い ✓

**4** 一番上に現れた **「共有シート」から［…］を受け取る** の青い部分 → **「PDF」だけをオン**

**5** 2つめの青いピルが **「ショートカットの入力」**、その次が **「テキスト」** か確認

**6** 検索欄に **`Base64`** → **「Base64エンコード」** → 右端の **⟩** を開いて **「行区切り」を「なし」**

**7** 検索欄に **`テキスト`** → 一番上の **「テキスト」** → 空欄に次を手入力

```
https://libertedebut.github.io/recipe-kago/#r=
```

カーソルを `=` のすぐ右に置いて、キーボードのすぐ上の帯から **「Base64エンコード」** をタップ。

**8** 検索欄に **`URLを開く`** → **「URLを開く」**

**9** ⌄ →「名称変更」で **「レシピかごに送る（PDF）」**

## できあがりの形

```
1  「共有シート」から［PDF］を受け取る
2  PDF［ショートカットの入力］から［テキスト］を取得
3  ［テキスト］を base64で エンコード（行区切り：なし）
4  テキスト  https://libertedebut.github.io/recipe-kago/#r=［Base64エンコード］
5  ［テキスト］を開く
```

## 写真版と違うところ

**「テキストを結合」を入れないでください。**

写真版には `@@@RECIPE@@@` で結合するアクションがありますが、**PDF版に入れると文章が途中で切れます。**
PDFから取り出した文字を結合アクションに通すと、265文字あたりで切断される現象を確認しています。

そのため、**PDF版は1つずつ**の取り込みになります。

## 使い方

1. Safariでレシピのページを開く
2. **スクリーンショット**を撮る → 左下のサムネイルをタップ
3. 上の **「フルページ」** を選ぶ → **「完了」** → **「"ファイル"に保存」**
4. あとで **ファイル**アプリを開き、**PDFを1つ選んで共有** → **「レシピかごに送る（PDF）」**

**PDFを2つ以上選ぶと「項目を選択」と聞かれます。** 1つずつ送ってください。
同じ日のリストに積み上がり、同じ材料は合算されます。

## 注意

**文字の層があるPDFだけ**が対象です。

- Safariのフルページスクリーンショット → **使えます**
- 本や雑誌を撮った写真をPDFにしたもの → 中身が画像なので**文字が取れません**

雑誌や本は**写真版**を使ってください。
